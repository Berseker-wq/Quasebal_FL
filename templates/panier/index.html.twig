{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
    <h1>Mon Panier</h1>

    {# Affichage des messages flash #}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success">{{ message }}</div>
    {% endfor %}
    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}

    {% if produitsPanier is empty %}
        <p>Votre panier est vide.</p>
    {% else %}
        <table>
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Nom</th>
                    <th>Prix unitaire</th>
                    <th>Quantité</th>
                    <th>Total</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                {% for produit in produitsPanier %}
                    <tr>
                        <td><img src="{{ asset(produit.image) }}" alt="{{ produit.nom }}" width="50"></td>
                        <td>{{ produit.nom }}</td>
                        <td>{{ produit.prix|number_format(2, '.', ',') }} €</td>
                        <td>{{ produit.quantite }}</td>
                        <td>{{ produit.totalProduit|number_format(2, '.', ',') }} €</td>
                        <td>
                            <a href="{{ path('app_panier_ajouter', { id: produit.id }) }}" class="btn btn-sm btn-outline-primary" title="Ajouter 1">➕</a>
                            <a href="{{ path('app_panier_retirer', { id: produit.id }) }}" class="btn btn-sm btn-outline-secondary" title="Retirer 1">➖</a>
                            
                            <a href="{{ path('app_panier_supprimer', {id: produit.id}) }}" class="btn btn-sm btn-outline-danger" title="Supprimer l'article">🗑</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <h3>Total : {{ total|number_format(2, '.', ',') }} €</h3>

<a href="{{ path('app_panier_valider') }}" class="btn btn-success mt-3">✅ Valider le panier</a>

    {% endif %}
{% endblock %}
